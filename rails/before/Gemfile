source "https://rubygems.org"

ruby "~> 3.4"

gem "rails", "~> 8.0"
gem "puma", "~> 7.2"

# Required for distroless runtime images (DHI) that ship without system tzdata.
# Loaded on every platform so the gem is present in any container build.
gem "tzinfo-data"
